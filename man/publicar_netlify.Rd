% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilitaria_netlify.R
\name{publicar_netlify}
\alias{publicar_netlify}
\title{Publica un directorio a un sitio de Netlify existente}
\usage{
publicar_netlify(dir, site_id, token, timeout = 60)
}
\arguments{
\item{dir}{(char) Carpeta local con el sitio completo a publicar (debe
incluir un \code{index.html}).}

\item{site_id}{(char) Site ID (también llamado Project ID en el
dashboard de Netlify) del sitio de destino.}

\item{token}{(char) Personal access token de Netlify.}

\item{timeout}{(numeric) Segundos antes de abortar la petición.}
}
\value{
(list, invisible) La respuesta de la API de Netlify, con el campo
\verb{$deploy_ssl_url} del sitio ya publicado.
}
\description{
Zipea \code{dir} y lo sube como un nuevo deploy de producción vía la API de
Netlify (\code{POST /sites/{site_id}/deploys}). Cada deploy reemplaza el
contenido completo del sitio con lo que traiga el zip -- por eso \code{dir}
debe contener el árbol completo que se quiere servir (todos los reportes
ya publicados más el nuevo), no solo los archivos de esta corrida.
}
\examples{
#notrun (publicar_netlify("site", Sys.getenv("NETLIFY_SITE_ID"), Sys.getenv("NETLIFY_AUTH_TOKEN")))
}
