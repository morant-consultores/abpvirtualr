% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analisis_graficar_preguntas.R
\name{etiqueta_semaforo}
\alias{etiqueta_semaforo}
\title{Traduce el código corto de nivel del semáforo a una etiqueta legible}
\usage{
etiqueta_semaforo(nombre_color)
}
\arguments{
\item{nombre_color}{(char) Código corto del nivel (\code{"vf"}, \code{"vc"}, \code{"a"},
\code{"rc"}, \code{"rf"}).}
}
\value{
(char) Etiqueta legible del nivel, o el código original si no
coincide con ninguno conocido.
}
\description{
El semáforo de brecha se distingue hoy solo por color (verde->rojo), lo
cual es problemático para audiencias con daltonismo rojo-verde. Esta
función mapea el \code{nombre_color} corto (\code{"vf"}, \code{"vc"}, \code{"a"}, \code{"rc"},
\code{"rf"}) que ya trae \code{\link[=calcular_brecha]{calcular_brecha()}} a una etiqueta de texto para
mostrarla junto al color (ver \code{\link[=generar_tabla]{generar_tabla()}}).
}
\examples{
etiqueta_semaforo(c("vf", "a", "rf"))
}
