% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resumidor.R
\name{separar_categorias}
\alias{separar_categorias}
\title{Separa el bloque de resumen en categoría + texto}
\usage{
separar_categorias(resumen)
}
\arguments{
\item{resumen}{(char) El string \code{Resumen} devuelto por \code{generar_resumen()}.}
}
\value{
(tibble) Columnas \code{categoria} y \code{texto}, una fila por categoría.
Si una sección no trae el formato \code{"**Categoria**"}, \code{categoria} queda
\code{NA} y \code{texto} conserva la sección completa. Tibble vacía si \code{resumen}
es \code{NA}/\code{NULL}/vacío.
}
\description{
\code{generar_resumen()} devuelve un solo string con secciones
\code{"**Categoria**\\ntexto"} unidas por \code{"\\n\\n&nbsp;\\n\\n"}. Esta función las
separa en una tibble para poder renderizar una lámina por categoría (p.ej.
en un reporte revealjs) en vez de una sola tabla larga y con scroll.
}
