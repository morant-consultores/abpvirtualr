% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analisis_resumir_ia.R
\name{resumir_respuestas}
\alias{resumir_respuestas}
\title{Resume un conjunto de respuestas de una pregunta vía la API de IA}
\usage{
resumir_respuestas(
  pregunta,
  respuestas,
  url = getOption("abpvirtual.api_url",
    "https://abp-flask-api.azurewebsites.net/api/v1/resumen_pregunta"),
  cache_dir = getOption("abpvirtual.cache_dir", tools::R_user_dir("abpvirtualr",
    "cache")),
  timeout = 30,
  reintentos = 2
)
}
\arguments{
\item{pregunta}{(char) Nombre/texto de la pregunta o tema a resumir.}

\item{respuestas}{(char) Vector de respuestas a resumir.}

\item{url}{(char) Endpoint de la API de resumen. Default
\code{getOption("abpvirtual.api_url")}.}

\item{cache_dir}{(char o NULL) Directorio donde cachear resúmenes como
\code{.rds} nombrados por hash de \verb{(pregunta, respuestas)}. \code{NULL} desactiva
el cache. Default \code{getOption("abpvirtual.cache_dir")} (por defecto,
\code{tools::R_user_dir("abpvirtualr", "cache")}).}

\item{timeout}{(numeric) Segundos antes de abortar la petición.}

\item{reintentos}{(int) Número de reintentos ante error transitorio
(5xx o de red), con backoff exponencial.}
}
\value{
(char) El resumen (\verb{$data} de la respuesta de la API). \code{NA_character_}
si \code{respuestas} está vacío (contrato "sin datos", ver Roadmap 2.5).
}
\description{
Unifica en una sola función el bloque \code{POST -> content -> fromJSON} que
antes se copiaba en cada script del caso (\code{api_resumidor.R},
\code{entregable.R}...), con timeout, reintentos con backoff exponencial (vía
\code{\link[httr2:req_retry]{httr2::req_retry()}}) y errores explícitos si la API responde un status
distinto de 200 o el JSON no trae el campo \code{data} esperado. Cachea el
resultado en disco por hash de \verb{(pregunta, respuestas)} para que un
re-render no vuelva a pagar el costo del LLM (ver \code{cache_dir}).
}
\examples{
#notrun (resumir_respuestas("¿Qué mejorarías?", c("más médicos", "mejor equipo")))
}
