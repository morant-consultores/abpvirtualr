{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); fp=d.get('file_path',''); sys.exit(1) if 'renv.lock' in fp else sys.exit(0)\"",
            "blocking": true
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,subprocess; d=json.load(sys.stdin); fp=d.get('file_path',''); subprocess.run(['Rscript','-e','devtools::document(quiet=TRUE)'],capture_output=True) if fp.endswith('.R') and '/R/' in fp else None\""
          }
        ]
      }
    ]
  }
}
