---
title: "Análisis de brechas percibidas"
subtitle: "Digital"  
author:   <img src="img/logo2.png" width="900" height="200" />
            "Gerencia del Poder"

date: '`r abpvirtualr::fecha_es()`'

output:
  xaringan::moon_reader:
    css: [xaringan-themer.css]
    nature:
      slideNumberFormat: "%current%"
      highlightStyle: github
      highlightLines: true
      ratio: 16:9
      countIncrementalSlides: true
---


class: inverse, middle, center

```{r, include = FALSE}
# Todos los campos vienen con default documentado (ver ?parametros_default).
# Para un caso nuevo, sobreescribe solo los valores de marca aquí abajo.
parametros <- abpvirtualr::parametros_default(
  primario = "#2A3D6E",
  familia = "Poppins"
)
```

```{r setup, include=FALSE}
options(htmltools.dir.version = FALSE)
knitr::opts_chunk$set(
  fig.width=9, fig.height=4, fig.retina=3,
  out.width = "100%",
  # cache = FALSE por default (a propósito): con datos vivos, cachear todo
  # el deck puede servir resultados viejos en un re-render. Solo el chunk
  # que trae los datos de BD (bd <- leer_base(...), más abajo) declara su
  # propio cache = TRUE explícito. Para invalidarlo (sesión nueva, datos
  # actualizados) borra el directorio `*_cache/` de este .Rmd.
  cache = FALSE,
  echo = F,
  message = FALSE,
  warning = FALSE,
  hiline = TRUE,
  fig.showtext = F
)


options(highcharter.google_fonts = TRUE)

```

```{r include=F, echo=F, results=F}
library(xaringanthemer)
style_mono_accent(base_color =  parametros$primario,
                  header_font_google = google_font(parametros$familia),
                  text_font_google   = google_font(parametros$familia),
                  code_font_google   = google_font(parametros$familia),
                  colors = c("rf" = parametros$sm_rf, "rc" = parametros$sm_rc,"a" = parametros$sm_a,"vc" = parametros$sm_vc, "vf" = parametros$sm_vf)
)

options(htmltools.dir.version = FALSE, htmltools.preserve.raw = FALSE)
```

```{r}
library(abpvirtualr)
```

```{r, cache = TRUE}
# Único chunk cacheado del deck: la consulta a la BD es lo caro de re-hacer.
# Borra `*_cache/` (o corre con id_sesion distinto) para forzar datos frescos.
thm <- tema_high(font = parametros$familia, color = "black", size = "15px")
bd <- leer_base( id_sesion = NULL)
```



# ETAPA 1: INICIO

---

`r slides_nubes(bd, etapa = 1, parametros, thm)`

class: inverse, middle, center


# ETAPA 2: BRECHA

---

`r slides_etapa_2(bd, top_p = 5, top_r = 2, parametros = parametros, thm = thm)`

class: inverse, middle, center



# ETAPA 3: CIERRE

---

`r slides_nubes(bd, etapa = 5, parametros, thm)`

class: inverse, middle, center

# Gracias
