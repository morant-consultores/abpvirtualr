---
title: " . "
subtitle: "Análisis de Brechas Percibidas <br> Digital"  
author:   <img src="img/logo2.png" width="900" height="200" />
            "Gerencia del Poder"

date: '`r Sys.setlocale(locale = "es_ES.UTF-8"); format(Sys.Date(),"%d de %B de %Y")`'

output:
  xaringan::moon_reader:
    chakra: libs/remark-latest.min.js
    css: [xaringan-themer.css]
    nature:
      slideNumberFormat: "%current%"
      highlightStyle: github
      highlightLines: true
      ratio: 16:9
      countIncrementalSlides: true

resource_files:
- R/analisis_graficar_preguntas.R
- R/analisis_procesar_pregunta.R
- R/desarrollo_preparar_etapas.R
- R/desarrollo_preparar_slides.R
- R/utilitaria_colectar_base.R
- data/conexion.rda
---


class: inverse, middle, center

```{r, include = FALSE}
parametros <- list(
  primario = "#2A3D6E",
  primario_claro = "#304E83",
  primario_obscuro = "#232B58",
  inverso = "#4C97C8",
  inverso_claro = "#88B9CF",
  cortes = seq(0,10000,length.out = 6),
  # cortes = c(0,4,7.5,11,16,25)*20^2,
  sm_vf = "#6A994E",
  sm_vc = "#A7C957",
  sm_a = "#F9C74F",
  sm_rc = "#D65A1F",
  sm_rf = "#B31D1F",
  familia = "Poppins",
  gris = "#343a40",
  gris_claro = "#ced4da",
  etiquetas = "14px"
)
```

```{r setup, include=FALSE}
options(htmltools.dir.version = FALSE)
knitr::opts_chunk$set(
  fig.width=9, fig.height=4, fig.retina=3,
  out.width = "100%",
  cache = T,
  echo = F,
  message = FALSE, 
  warning = FALSE,
  hiline = TRUE,
  fig.showtext = F
) 


options(highcharter.google_fonts = TRUE)
```

```{r include=F, echo=F, results=F}
library(xaringanthemer)
style_mono_accent(base_color =  parametros$primario,
                  header_font_google = google_font(parametros$familia),
                  text_font_google   = google_font(parametros$familia),
                  code_font_google   = google_font(parametros$familia),
                  base_font_size = "25px",
                  header_h1_font_size = "1rem",
                  header_h2_font_size = "2rem",
                  header_h3_font_size = "1.5rem",
                  colors = c("rf" = parametros$sm_rf, "rc" = parametros$sm_rc,"a" = parametros$sm_a,"vc" = parametros$sm_vc, "vf" = parametros$sm_vf)
)

options(htmltools.dir.version = FALSE, htmltools.preserve.raw = FALSE)
```

```{r}
library(dplyr)
library(ggplot2)
library(purrr)
library(dbplyr)
library(highcharter)
# library(readr)
# library(odbc)
# library(hexbin)
# library(abpvirtualr)

# devtools::load_all(here::here())

```

```{r, include = F}
list.files("R",full.names = T) %>% map(~source(.x))

thm <- tema_high(font = parametros$familia, color = "black", size = "15px")
bd <- leer_base( id_sesion = 3169)
```



## ETAPA 1: INICIO

---

`r slides_nubes(bd, etapa = 1, parametros, thm)`

class: inverse, middle, center


## ETAPA 2: BRECHA

---

`r if(nrow(bd$calif_cat) > 0) slides_etapa_2(bd, top_p = 5, top_r = 2, parametros = parametros, thm = thm)`

class: inverse, middle, center



## ETAPA 3: CIERRE

---

`r slides_nubes(bd, etapa = 5, parametros, thm)`

class: inverse, middle, center

## Gracias
